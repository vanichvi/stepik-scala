package excercises

object Compare extends App {
  def compare(v1: String, v2: String) = {
    //    val num1 = v1.split(".")
    //    val num2 = v2.split(".")
    //    num1 += "0"
    //    def loop(arr1, arr2a)
  }
}
